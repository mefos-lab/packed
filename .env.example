# Copy this file to .env and fill in real values. .env is gitignored — never commit it.

OPENFEC_API_KEY=                  # Required for fec_* tools — free key via https://api.data.gov/signup/
# LDA_API_KEY=                    # Not yet wired into server.py — LDA client is a future phase
